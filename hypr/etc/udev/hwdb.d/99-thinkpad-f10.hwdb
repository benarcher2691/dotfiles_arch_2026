# Remap F10 (XF86Bluetooth) to XF86Launch1 to prevent rfkill toggle
# This allows F10 to be bound in Hyprland for media controls
#
# Install: sudo cp this-file /etc/udev/hwdb.d/
# Apply:   sudo systemd-hwdb update && sudo udevadm trigger

evdev:name:ThinkPad Extra Buttons:dmi:bvn*:bvr*:bd*:svnLENOVO*:pn*:*
 KEYBOARD_KEY_48=prog1
